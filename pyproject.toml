[project]
name = "CombustionAgent"
version = "0.1.0"
description = "Library of an AI agent for combustion mechanisms"
authors = [
  { name="Grégoire Corlùy" }
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]